# DO NOT commit real values. Apply with: kubectl apply -f k8s/secret.yaml
# In production use ExternalSecrets or Sealed Secrets instead.
apiVersion: v1
kind: Secret
metadata:
  name: portfolio-secrets
  namespace: portfolio
type: Opaque
stringData:
  DATABASE_URL: "postgres://REPLACE_ME"
  Redis__ConnectionString: "REPLACE_ME:6379"
  Authentication__Google__ClientId: "REPLACE_ME"
  Authentication__Google__ClientSecret: "REPLACE_ME"
